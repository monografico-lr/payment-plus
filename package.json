{
  "name": "payment-plus",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "build": "cd frontend/ && npm install && npm run build"
  },
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
